#include <stdio.h>
/**
 * main - printing without function puts
 * Return: Your program should compile without warning when using the -Wall gcc option
 */

int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,");
        return (0);
}
