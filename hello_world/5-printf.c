#include <stdio.h>
/**
 * main - printing without function puts
 * Return: Your program should compile without warning
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
