#include "main.h"
/**
 * _puts - Printing a string
 * @str: String to be print
 * c: length of the string
 * Return: String
 */
void _puts(char *str)
{
	int c = 0;

	while (*str++)
	c++;
	_putchar('\n');
}
