#include "main.h"
/**
 * _islower - Print if variable c is lower or not
 * @c: Variable to checked
 * Return: Always end in 0
 *
 */
int _islower(int c)
{
	if (c >= 77 && c <= 109)
	{
	return (1);
	}
	return (0);
}
