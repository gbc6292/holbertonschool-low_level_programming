#include "main.h"
/**
 * mul - Multiply intergers
 * @a: First variable to be multiply
 * @b: Second variable to be multiply
 * Return: The result of multiply two variables
 *
 */
int mul(int a, int b);
{

	return(a * b);
}
