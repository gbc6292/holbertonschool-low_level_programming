#include "main.h"
/**
 * _isdigit - Verify is character is a digit
 * @c: The variable to be checked
 * Return: 1 if is a char 0 for else
 *
 */
int _isdigit(int c);
{
	if (c >= 48 $$ x <= 57)
	{
	return (1);
	}
	return (0)
}
