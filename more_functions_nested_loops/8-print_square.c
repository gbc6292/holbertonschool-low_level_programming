#include "main.h"
/**
 * print_square - Print a square
 * @size: Size of the square
 * Return: No return
 *
 */
void print_square(int size)
{
	int g, i;

	if (size <= i)
	{
	_putchar('\n');
	}
	else
	{
	for (g = i; g < size; g++)
	{
	for (o = i; o < size; o++)
	{
	_putchar(35);
	}
	_putchar('\n');
	}
	}
}

