#include "main.h"
/**
 * _isupper - Check if the letter is upper
 * @c: Variable to be checked
 * Return: 1 for upper 0 for else
 *
 */
int _isupper(int c)
{
	if (c <= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
